# @repo/schema

Shared validation schemas and TypeScript types using [Zod](https://zod.dev/).

## Usage
Provides schemas for payload validation and corresponding TypeScript types:

```typescript
import { loginSchema } from "@repo/schema";
```
